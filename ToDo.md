
---

## 📋 ToDo.md

### 🛠 運用ルール
1. **着手：** 「未着手」の項目を「作業中」に移動し、`ToDo.md`を更新する。
2. **完了：** 実装が完了したら項目を「完了」に移動し、`ToDo.md`を更新する。

重要！
日本語で応答する。
完了の報告をする前にエラーがないことを確認する

---

### 📥 未着手
*現在未着手の項目はありません。*

---

### 🚧 作業中
*現在作業中の項目はありません。*

---

### ✅ 完了

#### ■ 全体（追加分13）
- [x] 再起動するたびにウィンドウサイズが微妙に変わる → 保存時に `outerSize()`（装飾込み）を使い、復元時に `setSize()`（クライアント領域設定）を使っていたため、起動毎にタイトルバー＋枠分がドリフトしていた。保存側を `innerSize()` に変更して対称化
- [x] F11フルスクリーンでタスクバーは消えるがウィンドウの高さが変わらない → `decorations: false` の Windows では `setFullscreen(true)` がリサイズしない問題。`currentMonitor()` でモニタサイズを取得して `setSize`/`setPosition` で手動全画面化、`setAlwaysOnTop(true)` でタスクバーを覆う方式に変更。退出時は保存しておいた位置/サイズ（or 最大化状態）に復元

#### ■ 中央下ペイン：コンソールエリア（追加分13）
- [x] **Ollamaタブを追加** → ConsolePane.tsx に OllamaTab コンポーネント追加、Rust 側に ollama_installed/claude_installed/ollama_models コマンド追加
    - [x] Ollamaのインストールを確認。インストールされていない場合はインストールしてくださいと表示する。 → `ollama --version` で判定、未インストール時はダウンロードリンク付き案内を表示
    - [x] Claude Codeのインストールを確認。インストールされていない場合はインストールしてくださいと表示する。 → `claude --version` で判定、未インストール時はインストール案内を表示
    - [x] タブの中にはモデル選択のドロップダウンリスト、「起動」ボタン、コンソールを表示 → ツールバーに selector + プロンプト入力 + 起動/停止/再取得ボタン、下部にコンソール出力＋入力欄
    - [x] モデル選択のリストではOllamaでダウンロード済みのモデル一覧を表示 → `ollama list` をパースして NAME 列だけ抽出
    - [x] 「起動」ボタンを押すとコンソールで「ollama launch claude --model 選択したモデル名」で起動する。 → Command.create で spawn、stdout/stderr をコンソールに出力、stdin で対話可能
    - [x] `claude --print` が stdin を 3 秒しか待たず "Input must be provided..." で失敗する問題を修正 → 起動前に必須プロンプト入力欄を追加し、spawn 直後に stdin へ即書き込みする方式に変更

#### ■ 中央上ペイン：テキストエディタ（追加分12）
- [x] *.mdファイルの編集時にカーソル位置がずれる → CRLF ファイル（CLAUDE.md等）で `\n` 分割すると各行末に `\r` が残りスパン内でダブル改行になる問題を修正（`split(/\r?\n/)` に変更）。併せてオーバーレイの font-weight/font-style/word-break/right の不整合も修正

#### ■ 全体（追加分11）
- [x] 外部で追加変更されたファイルを反映する → notify クレートでディレクトリ監視、file-changed イベントで展開中フォルダを自動リロード（500ms デバウンス）

#### ■ 右ペイン：ウィジェットエリア（追加分11）
- [x] **バッテリーウィジェットの修正** → root\wmi BatteryStatus.PowerOnline でACアダプター接続を確実に検出、BatteryStatus=3（満充電）もフォールバックで対応

#### ■ 全体（追加分10）
- [x] 再起動時に前回の再現をする際にデフォルト画面が表示される → appStateReady が false の間はレンダリングを抑制してちらつきを防止

#### ■ 中央上ペイン：テキストエディタ（追加分10）
- [x] 検索結果表示で検索文字列を色を変えて強調 → 検索タブタイプ(search)を新規追加、検索クエリを黄色ハイライトで表示
- [x] 検索結果表示で検索結果の行をクリックしたときに該当するファイルを開く → 相対パスからベースディレクトリで絶対パスを構築し read_file + onFileOpen で表示
- [x] 表示するファイルが「*.md」の時にマークダウン用の強調表示 → オーバーレイ方式で見出し・太字・斜体・コード・リンク等を色分け表示

#### ■ 全体（追加分9）
- [x] 起動タイムアウトを10秒に修正 → App.tsx の setTimeout を 3000 → 10000 に変更
- [x] 最大化状態での再起動時のウィンドウサイズ問題修正 → isMaximized フラグを AppWindowState に追加、保存・復元時に最大化状態を正しく処理

#### ■ 左ペイン：ファイルリスト（追加分9）
- [x] 「検索」タブでフォルダが展開できない → クリックで selectSearchScope と toggleSearchDir を同時呼び出しに変更
- [x] 「開く」ボタンのテキスト削除 → `📁 開く` → `📁` アイコンのみに変更
- [x] 最近のフォルダを開くアイコンが無反応 → stopPropagation を追加してグローバルクリックハンドラーとの競合を解消
- [x] ボタン順番を「開く」「最近のフォルダ」「新規ファイル」「新規フォルダ」に変更

#### ■ 右ペイン：ウィジェットエリア（追加分9）
- [x] **システムモニターウィジェットの修正** → NPU使用率を常時表示、取得できない場合は0%で表示
- [x] **バッテリーウィジェットの追加** → BatteryWidget.tsx 新規作成
    - [x] バッテリーアイコン（残量に応じた塗り + 充電中⚡マーク）と残量%を表示
    - [x] 充電中の場合はアイコンに⚡マークを表示
    - [x] 使用中の場合はおおよその使用可能時間を表示

#### ■ 左ペイン：ファイルリスト（追加分8）
- [x] 「開く」ボタンの文字列の左側にフォルダのアイコンを追加 → 📁 アイコン付き「開く」ボタンに変更済み
- [x] 最近のフォルダを開くボタンを追加（🕐 アイコン） → dir-header に時計アイコンボタン追加、ドロップダウンメニューで過去5件表示
- [x] 最近のフォルダを開くボタンを押すと、過去５件分の履歴から選択してフォルダを開く → recentDirs を settings に保存、選択時に openDir 実行
- [x] 最近のフォルダを開くでフォルダを開いたときは、テキストエディターで前回そのフォルダを開いた時のタブを再現 → dirTabStates でフォルダごとのタブ状態を保存・復元
- [x] 「検索」タブにGrep機能の追加 → Rust に grep_files コマンド追加、再帰的にファイル内テキスト検索（大文字小文字無視、500件上限）
- [x] 「検索」タブを選択したときに現在のプロジェクトフォルダのツリー構造を表示（ファイルは表示しない） → list_directories_only コマンド追加、フォルダのみのツリー表示
- [x] 「検索」タブでフォルダを選択した場合はその配下のみを検索対象にする → searchScopeDir で選択フォルダを検索対象に設定
- [x] 「検索」タブでのGrepは検索対象以下のフォルダから指定された文字列を検索する → grep_files に対象ディレクトリを渡して再帰検索
- [x] 「検索」タブでのGrepは検索結果をテキストエディターの新規タブに表示する。タブ名は「検索：検索文字列」で表示 → handleGrepResult で「検索：{query}」タブを作成

#### ■ 全体（追加分7）
- [x] 起動時にファイルツリーも復元してから画面を表示 → LeftPane に onReady コールバックを追加し、ファイルツリー復元完了後に win.show() を実行（3秒タイムアウト付き）
- [x] フォルダを開きなおした場合はテキストエディターのタブを全部閉じる → handleDirChange でフォルダ変更検知時にタブ・履歴・カーソル位置をクリア

#### ■ 右ペイン：ウィジェットエリア（追加分7）
- [x] **システムモニターウィジェットの修正** → 各項目が null の場合は前回値を保持するマージ更新に変更、エラー時も stats を維持
- [x] **時刻ウィジェットの修正** → ClockWidget.tsx として独立ファイルに分離
- [x] **ALICEについてウィジェットの修正** → InfoWidget.tsx として独立ファイルに分離、「LM Studio 連携」を「Local AI 連携」に変更

#### ■ 全体（追加分6）
- [x] 「F11」キーでフルスクリーンモードになるようにしてください → App.tsx にキーイベントハンドラを実装済み
- [x] 非表示にしているウィジェットは処理も停止してください → visibleWidgets フィルターにより非表示ウィジェットはマウントされず、useEffect も実行されない設計で対応済み
- [x] 起動時に標準サイズ→前回サイズに変更が見える（再発）→ tauri.conf.json で visible: false に変更し、復元完了後に win.show() で表示
- [x] 起動時にポートを決め打ちにしている問題 → Vite のポートを 5174 (strictPort: true) に統一し、tauri.conf.json の devUrl と一致させた

#### ■ 中央上ペイン：テキストエディタ（追加分6）
- [x] タブを右クリックしたときのメニューに「他のタブをすべて閉じる」を追加 → コンテキストメニューUI を追加し、選択で他タブをすべて閉じる機能を実装

#### ■ 右ペイン：ウィジェットエリア（追加分6）
- [x] **システムモニターウィジェットの修正**
    - [x] AMDのGPUでVRAMの容量が正しくない → レジストリの HardwareInformation.qwMemorySize (64bit) から取得するように修正
    - [x] NPUの使用率が表示されない → NPU パフォーマンスカウンター検索を修正（ListSet で動的検索 + GPU Engine の engtype_NPU/Compute フォールバック）

#### ■ 左ペイン：ファイルリスト（追加分5）
- [x] ドラッグ移動で進入禁止マークが出る → file-list全体にonDrop追加、全DOM要素でpreventDefault+dropEffect="move"を徹底、空白部分はルートへ移動
- [x] 右クリックに「新規ファイル作成」と「新規フォルダ作成」を追加 → コンテキストメニューにstartCreate呼び出しを追加

#### ■ 右ペイン：ウィジェットエリア（追加分5）
- [x] **Claude Codeウィジェットの修正** → `claude auth status`のJSONをパースしプラン/アカウント/認証方法を表示、使用状況ページへのリンクボタン追加
    - [x] プラン種別の表示（Pro/Max等）
    - [x] アカウント情報の表示
    - [x] 「使用状況を確認」ボタンでブラウザの使用状況ページ(claude.ai/settings/usage)を開く
    - ※ 使用制限・リセット時間等の詳細情報はClaude APIに公開エンドポイントが存在しないため、ブラウザでの確認を案内

#### ■ 左ペイン：ファイルリスト（追加分4）
- [x] ドラッグ移動で進入禁止マークが出る → dragover全体でpreventDefault+dropEffect="move"、stopPropagation追加
- [x] 「+F」でファイル作成後に選択状態にして開く → commitCreate内でonFileOpen呼び出しを追加

#### ■ 右ペイン：ウィジェットエリア（追加分4）
- [x] **Claude Codeウィジェットの追加** → get_claude_usageコマンド(Rust)+ClaudeCodeWidget.tsxコンポーネント追加
    - [x] Claude Code の使用状態を表示するウィジェットの作成
    - [x] プランの使用制限の表示
    - [x] 週間制限の表示
    - [x] リセットされるまでの時間の表示
- [x] **システムモニターウィジェットの修正** → AMD GPU対応(PowerShell+WMIパフォーマンスカウンター)、NPU使用率追加
    - [x] AMDのGPUに対応（nvidia-smiフォールバック→PowerShellでWin32_VideoController+GPUパフォーマンスカウンター取得）
    - [x] NPUの使用率の追加（Windows NPU Utilizationパフォーマンスカウンター取得）

#### ■ 左ペイン：ファイルリスト（追加分3）
- [x] gitタブでpushしたあとでもPushボタンが押せる → git_has_unpushedコマンド追加、未プッシュコミットがなければボタンを無効化
- [x] gitタブからファイルタブに変更したときにスクロールしない → activeTab変更時にも選択ファイルへのスクロールをトリガー
- [x] gitタブでステータスが「変更」のファイルを選択したときにDiffタブを追加 → diff タブタイプ追加、使いまわしで表示
- [x] 「開く」「+F」「+D」のボタンがスクロールで消える → dir-headerをsticky化
- [x] +Fボタンでルートにファイルが作成される → 選択中ファイルの親ディレクトリに作成するよう修正
- [x] ファイルのドラッグ移動 → ドラッグ&ドロップでフォルダ間移動を実装（rename_path使用）
- [x] 右クリックメニューからファイル削除 → コンテキストメニュー追加、delete_pathコマンド追加
- [x] ドラッグ移動で進入禁止マークが出る → ファイルリスト全体でdragoverを許可、フォルダ判定を改善
- [x] 右クリックメニューが画面外に表示される → ref callbackでウィンドウ内に位置を自動調整

#### ■ 右ペイン：ウィジェットエリア（追加分3）
- [x] ニュース見出しに3行目のゴミが2px表示される → max-heightを削除し-webkit-line-clampのみで制御
- [x] ニュース見出しのゴミが依然残る → CSS line-clampを廃止、Canvas measureTextで実測しJS側で文字列を切り詰めて表示
- [x] 3行目が表示される見出しがある → 折り返しシミュレーションに変更（1文字ずつ追加し行送りを正確に計測）

#### ■ 中央下ペイン：コンソールエリア（追加分3）
- [x] AIチャットで「Failed to fetch」エラー → Rust経由のプロキシ(lm_chatコマンド)でCORS問題を解消
- [x] ログタブ削除・出力タブをログにリネーム → タブ構成を整理
- [x] OS標準シェルのコンソールタブ追加 → PowerShell/Zsh対応のShellTabコンポーネント追加

#### ■ 全体
- [x] 前回起動時の状態の再現ができていない → beforeunloadの非同期問題を修正（5秒ごとの定期保存を追加）
- [x] PowerShellから閉じるボタンでプロンプトに戻らない → メインウィンドウ閉じ時にapp.exit(0)で全体終了
- [x] ウィンドウ位置・サイズが再現できない → Tauri権限(set-size/set-position等)を追加
- [x] ファイルリストの展開状態が再現できない → レースコンディション修正（openDirと展開復元を分離）
- [x] テキストエディタのカーソル位置が再現できない → EditorPaneにcursorPositions復元処理を追加
- [x] ファイルリストでの選択状態が再現できない → scrollIntoViewで選択ファイルに自動スクロール
- [x] 起動時に標準サイズ→前回サイズに変更が見える → ウィンドウを不可視で起動し復元後に表示
- [x] gitとの連携機能を追加 → Git status/stage/unstage/commit/diff機能を実装

#### ■ 中央上ペイン：テキストエディタ
- [x] 画像表示時に幅と高さを表示 → ツールバーに「1920 x 1080 pix」形式で表示
- [x] 画像の拡大縮小（マウスホイール対応） → ズーム10%〜500%、ホイール操作対応
- [x] タブ閉じ時に前のタブを選択 → タブ選択履歴を記録し前のタブに戻る
- [x] 画像の拡大縮小がおかしい（100%表記が不正確） → Fitモード導入、実ピクセル基準のズーム%表示に修正
- [x] 画像のドラッグスクロール対応 + スクロールバー修正 → マウスドラッグでパン操作、grab/grabbingカーソル対応
- [x] 画像を拡大して上と左にはみ出た分がスクロールしても表示できない → flex中央揃えをやめてinner divで制御

#### ■ 右ペイン：ウィジェットエリア
- [x] ニュースOR検索 → 各キーワード個別取得→マージ→pubDateソート→最新5件
- [x] ニュース見出し3行目ゴミ表示 → max-height+word-break追加
- [x] ニュースウィジェットの見出しにゴミが残っている → HTML実体参照デコード処理をRust側に追加

#### ■ 左ペイン：ファイルリスト
- [x] 選択中ファイルへの自動スクロール → data-filepath属性+scrollIntoView
- [x] gitタブの追加（ファイル、Git、検索の順番） → GitPaneコンポーネント新規作成
- [x] gitタブで表示されるファイル名がおかしい → 相対パス全体表示+クォートされたパスのデコード対応
- [x] gitタブでコミットしたあとにpushする方法がない → Pushボタン追加（リモート存在時のみ表示）、git_push/git_has_remoteコマンド追加

#### ■ 設定画面
- [x] ウィジェットのON・OFF並べ替えに写真ウィジェットとニュースウィジェットがない → loadSettings時に不足ウィジェットをマージする処理を追加
- [x] ニュースウィジェット：更新間隔を追加(分単位、デフォルト30分) → newsIntervalフィールド追加、設定UIも追加

#### ■ 右ペイン：ウィジェットエリア
- [x] 写真ウィジェットが表示されない → 上記設定マージにより修正
- [x] ニュースウィジェットが表示されない → 上記設定マージにより修正
- [x] ニュースクリックで進入禁止マーク → iframeではなくシステムブラウザで開くように変更
- [x] 写真の切り替えがおかしい → フェードアウト完了後にフェードインする順次トランジションに修正

#### ■ 全体
- [x] 前回起動時の状態を可能な限り再現する
    - [x] ウィンドウ位置
    - [x] ウィンドウサイズ
    - [x] 各ペインのサイズ
    - [x] ファイルリストのフォルダの展開状態
    - [x] テキストエディタで開いていたファイル
    - [x] テキストエディタで選択中のファイル
    - [x] テキストエディタのカーソル位置

#### ■ 設定画面（追加分）
- [x] ファイルリスト：隠しファイルの表示(ON,OFFスイッチ)ONのときは隠しファイルを表示する
- [x] ウィジェットエリア：写真ウィジェットの写真フォルダを設定(フォルダ選択ダイアログ)
- [x] ウィジェットエリア：写真ウィジェットの写真の表示時間(秒数)ここで指定した時間写真を表示してから次の写真に切り替える
- [x] ウィジェットエリア：ニュースウィジェットのキーワードを登録(複数件登録可能)

#### ■ 中央上ペイン：テキストエディタ（追加分2）
- [x] 画像やWebを表示する場合はタブ内にブラウザを起動(or表示？)する。

#### ■ 右ペイン：ウィジェットエリア（追加分2）
- [x] **天気ウィジェットの修正**
    - [x] 更新時に一度表示が「取得中」になって天気が消えるのを修正
    - [x] レイアウトを変更。天気名を現在地の右に。最高気温と最低気温を縦に並べて現在気温の右に移動。

- [x] **写真ウィジェットの追加**
    - [x] 設定画面で設定されたフォルダ内の写真のスライドショーを行う(順番はランダム)
    - [x] ウィジェットの形は正方形で写真はその範囲に全体が入るように表示。トリミングはしない
    - [x] フェードイン・フェードアウトで写真を切り替える
    - [x] 写真をクリックした場合にテキストエディタに新規タブでブラウザを起動しそこに写真を表示する

- [x] **ニュースウィジェットの追加**
    - [x] 設定画面で登録された単語に関連するニュースをインターネットで取得して表示する
    - [x] ニュースは最新の５件の見出しをウィジェット内に表示
    - [x] ニュースをクリックするとテキストエディタに新規タブでブラウザを起動しそこに該当するWebページを表示する

#### ■ 中央下ペイン：コンソールエリア（追加分2）
- [x] 現在プロジェクトフォルダに設定されているフォルダをClaude Codeのカレントディレクトリに設定する

#### ■ 全体
- [x] 設定画面の追加（別ウィンドウで表示）
- [x] 各ペインのサイズを境界線のドラッグで変更できるように修正
- [x] タイトルバーの色合いを他UIと統一
- [x] タイトルバーのドラッグでのウィンドウの移動ができないのを修正
- [x] ウィジェットエリアとコンソールエリアの境界線を追加

#### ■ 左ペイン：ファイルリスト
- [x] 「フォルダを開く」ボタンの実装（フォルダ選択ダイアログを表示）
- [x] 選択したフォルダをプロジェクトフォルダとして設定
- [x] ファイル選択時、中央上ペインに内容を表示
- [x] すでにファイルが開かれている場合、新規タブで表示

#### ■ 中央上ペイン：テキストエディタ
- [x] 行番号を表示する
- [x] 保存後もタブ表示されるファイル名の先頭に・が表示されているので修正
- [x] 入力文字数を表示する。日本語の文字コードを考慮して正しい文字数を表示
- [x] 選択中のタブに対応するファイルを、ファイルリスト側でも選択状態にする

#### ■ 中央下ペイン：コンソールエリア
- [x] コンソールタブの追加（AI チャット / 出力 / ログ）
- [x] Claude Codeのタブを追加。Claude Codeを起動できるようにする
- [x] コンソールタブの順番の変更（Claude Code / AI チャット / 出力 / ログ）

#### ■ 右ペイン：ウィジェットエリア
- [x] **カレンダーウィジェットの修正**
    - [x] カレンダーをタブからウィジェットに変更。位置は時刻の下。
    - [x] 他のウィジェットと同じくタイトル(カレンダー)を追加。
- [x] **天気ウィジェットの追加**
    - [x] ウィジェット内に天気を追加。位置はカレンダーの下。
    - [x] インターネット経由で現在地の天気を取得
    - [x] 線画アイコンによる天気表示
    - [x] 気温表示（現在・最高・最低）
- [x] **天気ウィジェットの修正**
    - [x] 現在地が間違っている → navigator.geolocation (Windows Location Service) を優先使用に修正
- [x] **システムモニターウィジェットの追加**
    - [x] CPU使用率の表示(%)
    - [x] CPU温度の表示(°C)
    - [x] メモリ使用率の表示(%)
    - [x] GPU使用率の表示(%) ※NVIDIA GPU のみ
    - [x] GPU温度の表示(°C) ※NVIDIA GPU のみ
    - [x] VRAM使用率の表示(%) ※NVIDIA GPU のみ

#### ■ 左ペイン：ファイルリスト（追加分）
- [x] 初回起動時等リストが空の時だけ「フォルダを開く」ボタンを表示
- [x] ２回目以降の起動時には前回開いていたフォルダを再度表示
- [x] 選択されたフォルダ以下をツリー表示する(初期状態ではフォルダは閉じている。クリックで展開)
- [x] ファイルを選択すると、テキストエディタにタブが追加されるが、もう一度ファイル方部を選択しないとファイルの中身が表示されないのをファイル選択時に中身が表示されるように修正
- [x] ファイルの新規作成ボタンを追加
- [x] フォルダの新規作成ボタンを追加
- [x] ファイル及びフォルダの名前を変更できるように修正(WindowsならF2、MacならEnterのようにOS標準の操作でリネーム状態にする)

#### ■ 中央上ペイン：テキストエディタ（追加分）
- [x] 保存ボタンの廃止(Ctrl+Sのショートカット機能は残す)

#### ■ 中央下ペイン：コンソールエリア（追加分）
- [x] Claude Codeが起動ボタンを押してもエラーになり起動しないのを修正

#### ■ 右ペイン：ウィジェットエリア（追加分）
- [x] **システムモニターウィジェットの修正**
    - [x] CPU使用率の表示(%)がタスクマネージャーの値と違いすぎる問題を修正（永続Systemインスタンス + global_cpu_usage使用）
- [x] **カレンダーウィジェットの修正**
    - [x] 日本の祝日の日付が赤になっていない問題を修正（春分の日・秋分の日・ハッピーマンデー・振替休日に対応）

#### ■ 設定画面
- [x] **アプリの設定を保存(MacやLinux版の展開を考慮した保存方法)**
    - [x] テキストエディタ：オートセーブ(ON,OFFスイッチ)ONだと、現在編集中のファイルを自動で保存する。
    - [x] テキストエディタ：フォントサイズ(数値入力)テキストエディタ内だけに反映されるフォントサイズ。
    - [x] ウィジェットエリア：各ウィジェットの表示のON・OFF
    - [x] ウィジェットエリア：各ウィジェットの表示順番の変更

---
